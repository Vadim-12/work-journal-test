services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: work_journal
      MYSQL_USER: journal
      MYSQL_PASSWORD: journal
    ports:
      - '3306:3306'
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ['CMD', 'mysqladmin', 'ping', '-h', 'localhost']
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build: ./backend
    ports:
      - '3000:3000'
    environment:
      TZ: Europe/Moscow
      DB_HOST: mysql
      DB_PORT: 3306
      DB_USER: journal
      DB_PASSWORD: journal
      DB_NAME: work_journal
      PORT: 3000
    depends_on:
      mysql:
        condition: service_healthy
    healthcheck:
      test: ['CMD', 'wget', '-qO-', 'http://localhost:3000/work-types']
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 20s

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: /api
    ports:
      - '8080:80'
    depends_on:
      backend:
        condition: service_healthy

volumes:
  mysql_data:
